apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: metrics-stub
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: metrics-stub
  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s