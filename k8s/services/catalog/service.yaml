apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: ecom
  labels:
    app: catalog-service
    app.kubernetes.io/name: catalog-service
spec:
  type: ClusterIP
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
      name: http
  selector:
    app: catalog-service
