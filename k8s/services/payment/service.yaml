apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: ecom
  labels:
    app: payment-service
    app.kubernetes.io/name: payment-service
spec:
  type: ClusterIP
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
      name: http
  selector:
    app: payment-service
