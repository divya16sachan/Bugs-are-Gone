apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ecom

resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  # Note: Apply trigger-auth.yaml and scaledobject.yaml once KEDA operator is installed:
  # - trigger-auth.yaml
  # - scaledobject.yaml
