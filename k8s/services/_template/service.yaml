apiVersion: v1
kind: Service
metadata:
  name: SERVICE_NAME-service
  namespace: ecom
  labels:
    app: SERVICE_NAME
    app.kubernetes.io/name: SERVICE_NAME
spec:
  type: ClusterIP
  ports:
    - port: SERVICE_PORT
      targetPort: SERVICE_PORT
      protocol: TCP
      name: http
  selector:
    app: SERVICE_NAME
